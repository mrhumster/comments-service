apiVersion: v1
kind: Service
metadata:
  name: comments-reader
  namespace: go-app
spec:
  type: ClusterIP
  selector:
    app: comments-reader
  ports:
    - name: http
      port: 8080
      targetPort: http